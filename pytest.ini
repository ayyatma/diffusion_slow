[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    gpu: tests that use CUDA when available
    slow: tests that are still cheap, but instantiate MobileViT or run backward
